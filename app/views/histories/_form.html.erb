<%= form_for(@history) do |f| %>
  <% if @history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@history.errors.count, "error") %> prohibited this history from being saved:</h2>

      <ul>
      <% @history.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sake_id %><br />
    <%= f.number_field :sake_id %>
  </div>
  <div class="field">
    <%= f.label :member_id %><br />
    <%= f.number_field :member_id %>
  </div>
  <div class="field">
    <%= f.label :drunk %><br />
    <%= f.check_box :drunk %>
  </div>
  <div class="field">
    <%= f.label :next_order %><br />
    <%= f.check_box :next_order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
